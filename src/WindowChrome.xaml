<?xml version="1.0" encoding="utf-8" ?>
<!--  This is the chrome (as in the window around the WebContent, it is the browser window itself (borders, navigation, address bar, etc).  -->
<!--  Not to be confused with the browser from Google  -->
<Window
    x:Class="Horizon.WindowChrome"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:favorites="using:Horizon.Core.Favorites"
    xmlns:local="using:Horizon.Controls.Tabs"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkitc="using:CommunityToolkit.WinUI.Controls"
    xmlns:viewmodels="using:Horizon.ViewModels"
    mc:Ignorable="d">
    <Grid x:Name="RootWindowGrid" SizeChanged="RootWindowGrid_SizeChanged">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="188" />
        </Grid.ColumnDefinitions>
        <ContentPresenter
            x:Name="TabContentHost"
            Grid.Row="1"
            Grid.Column="0"
            Margin="0,-48,0,0"
            x:FieldModifier="public" />
        <Grid
            Grid.Row="0"
            Width="32"
            Height="40"
            HorizontalAlignment="Left"
            Background="Transparent">
            <Image
                Grid.Column="0"
                Width="26"
                Height="26"
                Margin="7,7,0,0"
                HorizontalAlignment="Left"
                VerticalAlignment="Top"
                Source="/Assets/AppIcons/Square44x44Logo.png" />
        </Grid>
        <Grid
            x:Name="BrowserControlIsland"
            Grid.Row="0"
            Width="250"
            Margin="100,0,0,0"
            HorizontalAlignment="Center"
            CornerRadius="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button
                Grid.Column="0"
                Height="24"
                Margin="2,0,2,0"
                Click="ToolbarButton_Click"
                Style="{StaticResource QuietButton}">
                <FontIcon FontSize="14" Glyph="&#xE712;" />
                <Button.Flyout>
                    <MenuFlyout Placement="Bottom">
                        <MenuFlyoutItem
                            Click="ToolbarFlyoutItem_Click"
                            Tag="ReadingMode"
                            Text="Reading mode">
                            <MenuFlyoutItem.Icon>
                                <FontIcon FontSize="16" Glyph="&#xE736;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem
                            Click="ToolbarFlyoutItem_Click"
                            Tag="Translate"
                            Text="Translate">
                            <MenuFlyoutItem.Icon>
                                <FontIcon FontSize="16" Glyph="&#xE775;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem
                            Click="ToolbarFlyoutItem_Click"
                            Tag="AddFavoriteFlyout"
                            Text="Add to favorites">
                            <MenuFlyoutItem.Icon>
                                <FontIcon FontSize="16" Glyph="&#xE734;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem
                            Click="ToolbarFlyoutItem_Click"
                            Tag="Downloads"
                            Text="Downloads (Ctrl + J)">
                            <MenuFlyoutItem.Icon>
                                <FontIcon FontSize="16" Glyph="&#xE118;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                        <MenuFlyoutItem
                            Click="ToolbarFlyoutItem_Click"
                            Tag="GenQRCode"
                            Text="Generate QR Code">
                            <MenuFlyoutItem.Icon>
                                <FontIcon FontSize="16" Glyph="&#xED14;" />
                            </MenuFlyoutItem.Icon>
                        </MenuFlyoutItem>
                    </MenuFlyout>
                </Button.Flyout>
            </Button>
            <Button
                Grid.Column="1"
                Height="24"
                Margin="5,0,0,0"
                Padding="0,-5,0,0"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Center"
                Click="UrlBoxButton_Click"
                Content="{x:Bind SelectedTab.Domain, Mode=OneWay}" />
            <Button
                Grid.Column="2"
                Height="24"
                Margin="2,0,2,0"
                Click="ToolbarButton_Click"
                Style="{StaticResource QuietButton}"
                Tag="CopyLink">
                <FontIcon FontSize="14" Glyph="&#xE71B;" />
            </Button>
        </Grid>
        <Grid
            x:Name="Sidebar"
            Grid.Row="1"
            Grid.Column="1"
            Margin="2,-5,8,3"
            x:FieldModifier="public">
            <Grid.RowDefinitions>
                <RowDefinition Height="40" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <Button
                    AutomationProperties.Name="New tab"
                    Click="ToolbarButton_Click"
                    Style="{StaticResource QuietButton}"
                    Tag="NewTab">
                    <Button.Content>
                        <FontIcon FontSize="16" Glyph="&#xE710;" />
                    </Button.Content>
                </Button>
                <AppBarSeparator />
                <Button
                    AutomationProperties.Name="New InPrivate tab"
                    Click="ToolbarButton_Click"
                    Style="{StaticResource QuietButton}"
                    Tag="NewPrivateTab">
                    <Button.Content>
                        <FontIcon FontSize="16" Glyph="&#xE727;" />
                    </Button.Content>
                </Button>
                <AppBarSeparator />
                <Button
                    x:Name="FavoritesBtn"
                    AutomationProperties.Name="Open favorites flyout"
                    Click="FavoritesFlyoutButton_Click"
                    Style="{StaticResource QuietButton}">
                    <Button.Content>
                        <FontIcon FontSize="16" Glyph="&#xE728;" />
                    </Button.Content>
                    <Button.Resources>
                        <Flyout x:Name="FavoritesFlyout" Placement="Bottom">
                            <Grid Height="300">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <InfoBar
                                    Grid.Row="0"
                                    IsClosable="False"
                                    IsOpen="True"
                                    Message="Anything missing?"
                                    Severity="Warning" />
                                <Button
                                    Margin="0,0,10,0"
                                    HorizontalAlignment="Right"
                                    Click="ToolbarButton_Click"
                                    Content="Read more"
                                    Tag="ConvertFavs" />
                                <TextBlock
                                    Grid.Row="1"
                                    Margin="5,0,0,5"
                                    FontSize="20"
                                    FontWeight="SemiBold"
                                    Text="Favorites" />
                                <Button
                                    Grid.Row="1"
                                    HorizontalAlignment="Right"
                                    Click="ToolbarButton_Click"
                                    Tag="FavoritesManager"
                                    ToolTipService.ToolTip="Open favorites manager">
                                    <FontIcon Glyph="&#xE8A7;" />
                                </Button>
                                <ListView
                                    x:Name="FavoritesListView"
                                    Grid.Row="2"
                                    Width="300"
                                    ItemsSource="{x:Bind viewmodels:MainViewModel.MainVM.FavoritesList, Mode=TwoWay}"
                                    RightTapped="FavoritesListView_RightTapped"
                                    SelectionChanged="FavoritesList_SelectionChanged">
                                    <ListView.ItemTemplate>
                                        <DataTemplate x:DataType="favorites:FavoriteItem">
                                            <StackPanel Margin="-5,10,-5,10" ToolTipService.ToolTip="{x:Bind Url}">
                                                <TextBlock
                                                    FontSize="16"
                                                    FontWeight="SemiBold"
                                                    Text="{x:Bind Title}"
                                                    TextTrimming="CharacterEllipsis" />
                                                <TextBlock
                                                    FontSize="12"
                                                    Foreground="{ThemeResource SystemAccentColorDark1}"
                                                    Text="{x:Bind Url}"
                                                    TextTrimming="CharacterEllipsis" />
                                            </StackPanel>
                                        </DataTemplate>
                                    </ListView.ItemTemplate>
                                    <ListView.ContextFlyout>
                                        <CommandBarFlyout
                                            x:Name="FavoritesContextMenu"
                                            AlwaysExpanded="True"
                                            Placement="BottomEdgeAlignedLeft">
                                            <AppBarButton
                                                Click="FavContextItem_Click"
                                                Tag="Copy"
                                                ToolTipService.ToolTip="Copy link">
                                                <AppBarButton.Icon>
                                                    <FontIcon Glyph="&#xE16F;" />
                                                </AppBarButton.Icon>
                                            </AppBarButton>
                                            <AppBarButton
                                                Click="FavContextItem_Click"
                                                Foreground="Red"
                                                Icon="Delete"
                                                Tag="Delete" />
                                            <CommandBarFlyout.SecondaryCommands>
                                                <AppBarButton
                                                    Click="FavContextItem_Click"
                                                    Label="Copy link text"
                                                    Tag="CopyText">
                                                    <AppBarButton.Icon>
                                                        <FontIcon Glyph="&#xE16F;" />
                                                    </AppBarButton.Icon>
                                                </AppBarButton>
                                            </CommandBarFlyout.SecondaryCommands>
                                        </CommandBarFlyout>
                                    </ListView.ContextFlyout>
                                </ListView>
                            </Grid>
                        </Flyout>
                    </Button.Resources>
                </Button>
                <AppBarSeparator />
                <Button
                    x:Name="ToolbarMoreBtn"
                    AutomationProperties.Name="Open more options flyout"
                    Style="{StaticResource QuietButton}">
                    <Button.Content>
                        <FontIcon FontSize="16" Glyph="&#xE712;" />
                    </Button.Content>
                    <Button.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuFlyoutItem
                                Click="ToolbarFlyoutItem_Click"
                                Tag="History"
                                Text="History">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE81C;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem
                                Click="ToolbarFlyoutItem_Click"
                                Tag="Downloads"
                                Text="Downloads">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE896;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutSeparator />
                            <MenuFlyoutSubItem Text="Developer">
                                <MenuFlyoutSubItem.Icon>
                                    <FontIcon Glyph="&#xE943;" />
                                </MenuFlyoutSubItem.Icon>
                                <MenuFlyoutItem
                                    Click="ToolbarFlyoutItem_Click"
                                    Tag="Crashes"
                                    Text="Crashes" />
                                <MenuFlyoutItem
                                    Click="ToolbarFlyoutItem_Click"
                                    Tag="Flags"
                                    Text="Flags" />
                                <MenuFlyoutItem
                                    Click="ToolbarFlyoutItem_Click"
                                    Tag="GPU"
                                    Text="GPU Internals" />
                                <MenuFlyoutItem
                                    Click="ToolbarFlyoutItem_Click"
                                    Tag="Inspect"
                                    Text="Inspect" />
                                <MenuFlyoutItem
                                    Click="ToolbarFlyoutItem_Click"
                                    Tag="Modules"
                                    Text="Modules" />
                            </MenuFlyoutSubItem>
                            <MenuFlyoutSeparator />
                            <MenuFlyoutItem
                                Click="ToolbarFlyoutItem_Click"
                                Tag="WhatsNew"
                                Text="What's new?">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xF133;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem
                                Click="ToolbarFlyoutItem_Click"
                                Icon="Setting"
                                Tag="Settings"
                                Text="Settings" />
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>
            </StackPanel>
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Hidden">
                <ListView
                    x:Name="TabListView"
                    AllowDrop="True"
                    CanDrag="True"
                    CanReorderItems="True"
                    ItemsSource="{x:Bind viewmodels:MainViewModel.MainVM.Tabs, Mode=TwoWay}"
                    RightTapped="TabListView_RightTapped"
                    SelectionChanged="TabListView_SelectionChanged">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:Tab">
                            <Grid PointerPressed="TabItem_MouseEvent" ToolTipService.ToolTip="{x:Bind Title, Mode=OneWay}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBlock
                                    Grid.Column="1"
                                    Margin="-2,-2,0,0"
                                    VerticalAlignment="Center"
                                    Text="{x:Bind Title, Mode=OneWay}"
                                    TextTrimming="CharacterEllipsis" />
                                <Button
                                    Grid.Column="2"
                                    Margin="0,0,-4,0"
                                    HorizontalAlignment="Right"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    Click="CloseTabButton_Click"
                                    Style="{StaticResource SidebarButton}">
                                    <Button.Content>
                                        <FontIcon
                                            FontFamily="Segoe Fluent Icons"
                                            FontSize="16"
                                            Foreground="Gray"
                                            Glyph="&#xE711;" />
                                    </Button.Content>
                                </Button>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ContextFlyout>
                        <MenuFlyout x:Name="TabContextMenu" Placement="BottomEdgeAlignedLeft">
                            <MenuFlyoutItem
                                Click="TabCTXItem_Click"
                                Tag="Duplicate"
                                Text="Duplicate tab">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE923;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutSeparator />
                            <MenuFlyoutItem
                                Click="TabCTXItem_Click"
                                Tag="CloseBelow"
                                Text="Close tabs below" />
                            <MenuFlyoutItem
                                Click="TabCTXItem_Click"
                                Tag="CloseAbove"
                                Text="Close tabs above" />
                            <MenuFlyoutItem
                                Click="TabCTXItem_Click"
                                Tag="CloseOthers"
                                Text="Close all other tabs" />
                        </MenuFlyout>
                    </ListView.ContextFlyout>
                </ListView>
            </ScrollViewer>
        </Grid>
    </Grid>
</Window>
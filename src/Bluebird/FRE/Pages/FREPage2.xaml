<Page
    x:Class="Bluebird.FRE.Pages.FREPage2"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:Bluebird.FRE.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Background="{ThemeResource SystemControlAcrylicWindowBrush}"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <FontIcon FontSize="80" Glyph="&#xE721;" />
        </Grid>
        <Grid Grid.Column="1" Margin="0,0,20,0">
            <muxc:Expander
                HorizontalAlignment="Stretch"
                HorizontalContentAlignment="Stretch"
                Header="Search engine"
                IsExpanded="True">
                <ComboBox
                    x:Name="SearchEngineSelector"
                    Loaded="SearchEngineSelector_Loaded"
                    PlaceholderText="Select a search engine.." />
            </muxc:Expander>
            <Button
                x:Name="FinishSetupBtn"
                Margin="10"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Content="Finish" />
        </Grid>
    </Grid>
</Page>
